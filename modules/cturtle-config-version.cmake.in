set(PACKAGE_VERSION @cturtle_VERSION@)

if("${PACKAGE_FIND_VERSION_MAJOR}" EQUAL "@cturtle_VERSION_MAJOR@")
   if("${PACKAGE_FIND_VERSION_MINOR}" EQUAL "@cturtle_VERSION_MINOR@")
      set(PACKAGE_VERSION_MATCH TRUE)
   elseif("${PACKAGE_FIND_VERSION_MINOR}" LESS "@cturtle_VERSION_MINOR@")
      set(PACKAGE_VERSION_COMPATIBLE TRUE)
   else()
      set(PACKAGE_VERSION_UNSUITABLE TRUE)
   endif()
else()
   set(PACKAGE_VERSION_UNSUITABLE TRUE)
endif()
